export enum SupportedDeviceEnum {
    Dishwasher = 1, // Represents a dishwasher device
    GarageDoor = 2, // Represents a garage door device
    LivingRoomLights = 3, // Represents living room lights device
    InvalidDevice = -1 // Represents an invalid device
  }
  